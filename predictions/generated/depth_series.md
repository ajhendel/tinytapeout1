# Depth series, predicted

Generated by `tools/prereg.py` from `runs/wokwi/55-openroad-stapostpnr/nom_tt_025C_1v80/tt_um_ajhendel_evofab__nom_tt_025C_1v80.sdf`, corner `nom_tt_025C_1v80`, at commit
`2244669295755556034c64b228aaafa5dc68582a`. The SDF is from the CI build of commit `32bd0b9`, whose RTL is identical to this one.

**Regenerating reproduces this file exactly.** That is checked, not assumed: two
independent CI runs of identical RTL, `f00c762` and `32bd0b9`, produced
byte-identical extracted timing reports at all nine corners. The whole point of
this directory rests on that property, so it is verified rather than hoped.

**Model layer: the released sky130 Liberty view plus post place-and-route
extraction.** That is one of the three layers docs/MEASUREMENT_PROTOCOL.md names
and it is allowed to be wrong. Being wrong here does not indict SPICE and does
not indict Liberty alone; recording the layer is what keeps those separable.

Intervals are plus or minus 20% on a delay and plus or minus
10% on a ratio, for the reason given in `tools/prereg.py`. **No interval
is widened after a measurement.**


**This is the load bearing one.** Without a predicted slope the
converter's calibration is unfalsifiable: any measured tap count can be
turned into any delay by choosing a tap width afterwards. The slope
predicts a delay per stage in TAPS, and the tap width predicted in
`tdc.md` turns it into picoseconds. Both have to be right.

| quantity | predicted | interval |
|---|---|---|
| per stage delay, the slope | 41.6 ps (0.357 taps) | 33.3 to 50.0 ps |
| fixed offset, the intercept | 0.232 ns (2.0 taps) | 0.186 to 0.279 ns |
| worst residual of the straight line fit | 24.3 ps (0.21 taps) | under 1 tap |

**Slope with the launch and merge offsets subtracted: 40.6 ps per stage (0.348 taps), intercept 0.245 ns, residual 13.6 ps (0.12 taps).** The raw slope above is +2.6 percent against it. The four short points of this series launch from tree branch 2 and the 32 stage point from branch 4, so a per-branch delay difference lands on the longest lever arm and moves the slope rather than the intercept. **The corrected slope is the prediction**; the raw one is what a host that did not know about the tree would measure, and the gap between them is predicted too. tools/char_offsets.py writes the per-path table.

| depth | path | predicted |
|---|---|---|
| 2 | `inv1_d2` | 0.324 ns |
| 4 | `inv1_d4` | 0.401 ns |
| 8 | `inv1_d8` | 0.568 ns |
| 16 | `inv1_d16` | 0.874 ns |
| 32 | `inv1_d32` | 1.575 ns |

**The linearity is a separate prediction from the slope.** If the
series is not linear the intercept is not a constant and nothing
else on this chip can be quoted against it, so the case that would
invalidate the method is predicted rather than assumed away. The
residual above is predicted to stay under one tap.

**Depth 16 is built twice under two names**, `inv1_d16` and `load_0`, and the two are
predicted to agree within one tap. That is a free repeatability
check with no extra silicon, and a disagreement there is a
disagreement about the instrument, not about depth.

**Falsified by:** a measured slope outside its interval; a residual
above one tap, which would retire the constant intercept and with
it every number on this chip quoted against it; or the two depth
16 paths disagreeing by more than a tap, which would say the
instrument is not repeatable before any model is even consulted.
